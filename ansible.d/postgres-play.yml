---
- name: Postgres
  hosts: all
  become: true
  become_user: root

  tasks:
    - name: Install
      apt:
        package: postgresql
        state: latest
        update_cache: yes

    - name: Status
      service:
        name=postgresql
        state=started
        enabled=yes